#!/bin/bash

set -exo pipefail

bundle exec rubocop -D --parallel
bundle exec brakeman -q -z --no-summary --no-pager
bundle exec reek -c .reek.yml app/
yarn run lint && yarn run typescript:typecheck
bin/rails zeitwerk:check
