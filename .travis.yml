language: php
sudo: false
php:
  - 7.1
before_script:
  - cp ./api/.env.travis ./api/.env
  - ./CI/backend.sh
script:
  - ./CI/frontend.sh
after_success:
  - bash -c echo "after_success"
  - bash -c echo "It works!"
