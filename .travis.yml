language: php
sudo: false
php:
  - 7.1
before_script:
  - cp ./api/.env.travis ./api/.env
  - ./CI/backend.sh
script:
  - ./CI/frontend.sh
  - ./api/vendor/phpunit/phpunit/phpunit -c app --coverage-clover=coverage.xml
after_success:
  - bash <(curl -s https://codecov.io/bash)
